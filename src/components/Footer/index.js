import React from 'react';
import './styles.scss';

const Footer = props => {
    return (
        <footer className="footer">
            <div className="wrap">
                this is a footer you could put any info or links needed here
            </div>
        </footer>
    );
};

export default Footer;
